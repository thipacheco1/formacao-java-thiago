# Matriz de cobertura — Aula 127

| Conteúdo oficial | Experiência reconstruída |
|---|---|
| `public`, `private`, `protected` e sem modificador | Matriz interativa entre própria classe, pacote, subclasse externa e outro pacote |
| Expor o mínimo necessário | Simulação que alterna API intencional e campos públicos que quebram Pedido |
| Cliente com atributos privados | Fonte completa, App externa, saída e falha real ao acessar `nome` |
| Dinheiro com construtor privado | Comparador entre factory pública, construtor e normalização privados |
| Pedido com API pública pequena | Rastreamento da chamada pública até validação e mutação privadas |
| Métodos privados para leitura interna | Fluxo em três quadros e código completo de `Pedido` |
| Classe e método package-private | Fronteira visual entre `dominio.pedido` e `app`, com falha de import comprovada |
| Classe de topo | Classificador de `public`, package-private, `private` e `protected` no topo |
| Introdução a `protected` | Cinco consumidores e ressalva correta sobre acesso por herança em outro pacote |
| Tabela mental de acesso | Incorporada à matriz com significado de projeto de cada nível |
| Getters e setters com critério | Decisor de seis métodos que trata cada acesso como compromisso de API |
| IntelliJ e debug | Mock com seis frames atravessando os quatro níveis de acesso |
| Erros controlados | Seis fontes deliberadamente inválidas compiladas pelo validador |
| Oito erros comuns | Clínica separando exposição, sintaxe, acoplamento e modelagem |
| Desafio Contrato | Sete fontes navegáveis, Validador package-private, auxiliares private e oito testes |

## Garantias

- O conteúdo único da aula oficial foi preservado e reorganizado como decisões observáveis.
- A aula contém 20 fontes Java válidas, seis bloqueios reais de acesso e sete execuções verificadas.
- A App enxerga somente APIs públicas; regras e mutações permanecem protegidas dentro do domínio.
- As 11 etapas têm conclusão reversível, gate final, foco automático no roteiro móvel e CSS responsivo.

